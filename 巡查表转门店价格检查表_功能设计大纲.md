# 巡查表 → 门店价格检查表（总部供货渠道）转换功能大纲

> 目标：把软件生成的「燕京啤酒全国即时零售渠道产品价格巡查表」自动转换为总部供货渠道门店价格检查表（版式参照《昆明京东酒世界价格巡查0902.xlsx》），作为功能写入价格核查软件包。
> 转换范围：**单张巡查表 → 一张门店价格检查表**。

---

## 1. 名词与版式约定

| 术语 | 含义 |
|---|---|
| 门店挂牌 | 平台页面上展示的店铺名（同一实体店在不同平台挂牌名不同） |
| 片区（实体店） | 同位置跨平台合并后的实体店，如 富春花园仓店/店/云仓店/啤酒小站挂牌 → 富春花园 |
| 平台 | 美团闪购 / 淘宝闪购 / 京东秒送（含源表「京东闪送」） |
| 规格 | 燕京U8 500ml×6听 / 6瓶 / 12听 / 12瓶 |
| 理论成交价 | 成交单价 − 打包配送费（源表 M 列） |
| 合格线 | 12瓶/听=59.9，6瓶/听=29.9（可配置，见 §5） |

输出表版式（12 列）：
`A门店 | B产品 | C是否违规 | D/E/F 理论成交价(美/淘/京) | G/H/I 实际成交价(美/淘/京) | J/K/L 图片(美/淘/京)`

---

## 2. 输入解析

- 源表结构（16 列 A~P）：分公司、所属主要区域、区域内即时零售平台、店铺名称、平台在售燕京产品、产品原价、产品成交单价、商品优惠、满减活动、优惠卷、红包、打包配送费、产品理论成交价格、去除平台优惠价格、图片、备注。
- 取值规则：
  - 数值列用**缓存值**读取（M/N 列是公式 `=G-L` / `=G+J+K-L`）；
  - O 列图片为 `_xlfn.DISPIMG("ID_...",1)` 公式，解析出图片 ID；
  - 图片字节来自包内 `xl/cellimages.xml`（ID → rId → `xl/media/*.jpeg`）。
- 无售卖记录：店铺名存在、产品列为空、仅含图片 → 判定为无售卖（B 列文案可配：`无售整件 6听/瓶售卖`）。

## 3. 店名规整（归一化）

1. 剥离前缀：`闪购`、`秒送`、`自营秒送`、`自营`、`连锁`；
2. 剥离中缀：`-啤酒小站`、`啤酒小站`；
3. 括号规整：全角/半角统一，缺失右括号补齐；
4. 剥离行政区前缀：如 `盘龙区北辰中路店` → `北辰中路店`；
5. 剥离后缀（**按最长优先**）：`云仓店` > `仓店` > `云仓` > `店`；
6. 得到**片区键**；挂牌原名保留用于查证/日志。

> ⚠️ 坑：后缀剥离必须最长优先（`云仓店` 先于 `仓店`），否则「富春花园云仓店」会被截成「富春花园云」。

## 4. 行级一致性校验（可选，桌面版可用 Vision/RapidOCR）

- 对每张截图 OCR 识别其页面顶部店铺名；
- 与行内「店铺名称」比对；不一致时**以截图为准**修正并写入转换日志（本项目实测：软件生成文件存在整段行店铺名错配，价格与截图一致、仅店名错）。
- OCR 不可用时降级为信任源表 + 输出「待人工确认清单」。

## 5. 合并、去重与判定

### 5.1 片区合并（跨平台 + 跨挂牌）
- 规则（可配）：同一片区的 `仓店/店/云仓/云仓店` 挂牌与 `-啤酒小站` 挂牌视为同一实体店，合并为一行；
- 无售卖：同片区多平台多挂牌合并为一行（每平台图片保留一张）；
- 新出现的片区键/规则冲突 → 进「待确认清单」sheet 或日志，不静默丢弃。
- 店名规范映射表（输出用店名）按连锁维护，如京东酒世界示例：富春花园→京东酒世界（富春花园仓店）、南亚→京东酒世界（南亚云仓）……（人工可事后改）。

### 5.2 重复折叠
- 同（片区, 规格, 平台）多条截图：取**最低成交价**那条记录（其理论价与图片同取）；同价取先出现的；
- 同平台多挂牌价格冲突（如 富春花园店 6瓶 35.7 vs 秒送啤酒小站 29.9）默认取最低（可配置：取主挂牌价）。

### 5.3 是否违规（C 列）
- 判定基准：**理论成交价**；
- 合格线：12瓶/12听 ≥ 59.9，6瓶/6听 ≥ 29.9（`理论价 < 合格线 → 违规=是`，等于为合格）；
- 任一平台触发即「是」，否则「否」；无售卖行填 `/`；
- 合并后**必须重算**违规列（合并前的列值会失真）。

## 6. 输出组装

- 行序：有售卖片区按首次出现顺序，片区内按规格 `6听→6瓶→12听→12瓶`；无售卖行置尾；
- 无数据平台一律填 `/`（价格格与图片格），不留空格；
- 数值格式：整数不带小数点；文本/图片按 §7；
- 文件命名（可配）：`{城市}{渠道}{YYYYMMDD}价格检查表.xlsx`；
- （可选扩展）附「汇总」sheet：按平台/规格统计 抽查数、价格区间、主要零售价、违规数——对接总部 60/55 双口径合格率。

## 7. 图片写入（WPS 单元格图片机制，关键踩坑记录）

输出表必须保持与软件原生文件一致的 WPS cellimage 机制，否则 WPS 显示 `#REF!`：
1. 公式格式：`<c t="str"><f>_xlfn.DISPIMG("ID_xxx",1)</f><v>=DISPIMG("ID_xxx",1)</v></c>`；
2. `xl/cellimages.xml`：条目内 `cNvPr id` 必须**全局唯一**（用户原有与新增不得从 2 重复编号）；`r:embed` 与 rels、媒体文件一一对应闭环；
3. `xl/_rels/cellimages.xml.rels`：全部 Relationship 保留（旧条目不能丢）；
4. `[Content_Types].xml`：`Default jpeg` + `Override /xl/cellimages.xml`（wps content type）；
5. **不要用 openpyxl 整体重写含 cellimages 的工作簿**（会丢部件与 WPS 扩展命名空间）；推荐：以模板/源表为基底，`sheetData` 行区手术式替换 + 部件级增补；
6. 输出校验：zip 完整性、公式数=引用数⊆条目数、rId 闭环、媒体存在、XML 良构。

## 8. 软件集成方案

| 端 | 落点 |
|---|---|
| macOS 桌面 `screenshot_ocr/` | 新增 `converter/` 模块（纯函数：parse → normalize → merge → judge → render → validate），入口放主界面「导出门店价格检查表」；OCR 校验复用 Vision |
| Windows `screenshot_ocr_windows/` | 移植同规则模块，OCR 用 RapidOCR/ONNX |
| 共用 | 字段解析/价格规则/合并配置放三端共享层（与现有 OCR 规则同策略） |
| 配置 | JSON：合格线、平台列映射、连锁店名映射、SKU 顺序、无售卖文案、输出命名、是否 OCR 校验 |
| 审计 | 转换日志：输入文件+规则版本+合并决策+OCR 修正+待确认项 |

## 9. 测试与验收

- 单元：店名归一化、后缀最长优先、合并去重取最低、违规边界（= 合格线）、平台映射；
- 集成（黄金样例）：`output/巡查表_20260902_171538/价格巡查表_20260902_171538.xlsx` → 断言输出行数/违规数/结构（与人工验收的昆明京东酒世界 0902 样例对齐）；
- 图片：公式数=引用数、cellimages 闭环、WPS 打开无 `#REF!`（人工抽检）；
- 跨连锁：歪马送酒、捣蛋鹿送酒等挂牌无同片区多后缀场景 → 行为=原样输出不合并。

## 10. 待确认清单（实现前）

1. 连锁/店名合并映射表是否由总部统一维护（跨省五省区是否同一套）；
2. 合格线分省/分产品差异（总部汇总有 55/60 双口径）；
3. 输出目录约定（是否按 `每日文件留档/月度汇总` 自动归档）；
4. 无售卖文案、待确认清单的呈现方式（单独 sheet vs 日志）。
