# 开发日志

## 2026-09-02 · 京东闪送截图识别

### 新增
- 京东闪送结算页解析补全：应付总额、商品金额、运费行、店铺卡片、商品划线价的识别与回填。
- 规格无法从截图可靠识别（标题截断、数量走默认）时，识别完成后弹出「产品规格补齐」窗口，可逐行选择 500ml*6听 / 6瓶 / 12听 / 12瓶；选择后回写产品名并清除待核对标记，选「维持默认」保留待核对高亮。
- 规格存疑行进入「待核对」筛选与导出前核对提醒。

### 修复
- 应付总额被 OCR 拆成「¥29.」+「.9 共减¥4.3」两个碎片时，整行拼接并归一化双小数点，正确还原 29.9。
- 「已免运费 ¥4.3¥0」实付运费为 0 元，此前误取划线原价 4.3；改为取行内最后一个金额（含 ¥0），与「活动减5元运费 ¥5.8¥0.8」取 0.8 的口径一致。
- 「燕京U8瓶装啤酒」的型号数字 8 被误当作规格数量；「500ml*6 默认」截断标题的数量无法提取。前者已拦截，后者新增 `ml*N` 兜底提取。
- 店铺名前缀清理：「自营 秒送 京东酒世界（XX店）」等 OCR 合读的徽标不再污染店铺名。
- macOS `field_parser.py` 一处未完成编辑导致的缩进错误修复（此前京东解析一直用的是旧字节码缓存）。
- 规格补齐回写改为替换完整容量与数量，避免选择规格后出现 `500ml500ml*6瓶`。
- 仅识别到“已免运费”但 OCR 漏掉实付 `¥0` 时，仍按 0 元运费处理。
- 识别京东“优惠券”行并写入平台优惠券字段，避免去除平台优惠价计算遗漏。

### 验证
- 使用 5 张京东闪送真实截图完成 macOS Vision OCR 复核：平台、店铺、价格和运费字段均已输出；标题截断或数量缺失时保留“产品规格需人工确认”。
- Windows pytest 回归 32 项通过；学习库脚本 34 项通过，`compileall` 与差异检查通过。

## 2026-08-29 · 1998 分省价格标准

### 调整
- 漓泉 1998 的 500ml*12 瓶/12 听按省份分别计算：广东合格线 70 元、第二档 65 元；广西合格线 60 元、第二档 55 元。
- 分省、分地级市汇总以及总结话术均使用对应省份的合格线和第二档线；9 装规格继续沿用 45 元规则。
- 产品选择提示、README 和 Windows 操作手册同步更新。

## 2026-08-28 · 取消城市设置的跨城误匹配修复

### 修复
- OCR 完成后的城市范围弹窗，点击“暂不设置城市”不再退化为全量城市匹配。
- 未选择城市范围时跳过本地自动匹配，避免同名店铺被写入其他城市的历史记录。
- 用户可继续通过“一键设置城市”或“联网识别城市”明确处理未设置城市的店铺。

## 2026-08-28 · 城市批量设置外置

### 调整
- 恢复「一键设置城市」独立按钮，与「联网识别城市」「查重核查」并列显示。

## 2026-08-28 · 表头主题字体色统一

### 调整
- 汇总表和核查明细表的标题、表头字体改用巡查表模板相同的“主题浅色 1”，与模板的蓝底表头保持完全一致。

## 2026-08-28 · 汇总表可读性优化

### 优化
- 汇总表的长统计表头固定分为两行显示，并启用自动换行，避免相邻列文字遮挡。
- 合格标准说明行加高并启用自动换行；分省、分地级市汇总表头行统一加高，低缩放查看时仍可完整阅读。

### 修复
- 分地级市汇总的合格标准说明行合并范围由 A:N 修正为 A:O，与实际 15 列数据对齐。

### 验证
- 新增汇总表版式回归测试，覆盖表头换行、行高、说明行换行和地级市合并范围。

## 2026-08-26 · macOS 共享逻辑同步 · 1.1.2

### 新增
- Windows 同步知识库投喂校验：仅处理同时含“区域”和“店铺”字段的巡查明细表，自动跳过汇总表；规格、表头和纯数值不再作为店铺入库。
- 知识库增加异常投喂数据预览与确认清理，删除时会一并清除关联别名、城市匹配和投喂修正记录。
- 联网城市识别本地留存用户授权时间、请求时间、允许城市范围、候选城市和最终裁决；联网仅发送店铺名称。

### 调整
- 旧版 `shop_city.db` 只作为历史迁移参考，不再自动填入城市。自动匹配仅采纳人工确认或人工投喂的学习记录，避免历史规格误入店铺库后造成跨城误判。
- 将低频操作收进“导出与汇总”和“数据管理”菜单；“联网识别城市”“查重核查”保留为外置按钮，并统一操作按钮高度。

### 验证
- Windows 静态编译、12 项单元回归、34 项学习库回归和离屏界面检查通过。

## 2026-08-26 · 联网城市识别准确性优化 · 1.1.1

### 修复
- 百度地图搜索先保留完整店铺名及括号内分店信息，只有无结果时才逐步使用短名称回退，避免丢失“罗湖店”“金贸店”等关键定位线索。
- 保留地图 POI 名称、原始排序和搜索词；城市不再按字母顺序任选，而是按完整店名、分店名、POI 相似度、候选排序和限定城市范围综合评分。
- 同名店铺、多城市分差过小或 POI 名称无关时不再自动填入城市，降低错误自动回填风险。

### 新增
- 联网识别完成后集中展示低置信度候选，默认“暂不填入”；用户逐项确认的城市才会写入表格和知识库，并记录为人工来源。
- 新增完整分店名查询、POI 证据优先级、歧义候选拦截和弱候选拦截回归测试。

### 兼容性
- 保留用户所选城市范围作为强约束，本地高可信城市匹配及现有 RapidOCR 打包链路不变。

## 2026-08-23 · Windows 与 macOS 功能粒度统一 · 1.1.0

### 新增
- OCR 支持取消；未完成或失败的图片会保留为待重试项，可单独重试且不覆盖其他人工修正。
- 识别结果增加待核对筛选、行级原因提示和成功/待核对计数。
- 导出前检查区域、店铺、产品和关键价格，允许用户确认后再导出。
- Windows 按钮统一边框、高度和禁用态样式，保留 RapidOCR、启动自检、联网识别、查重、汇总和话术能力。

### 兼容性
- Windows 端继续使用 RapidOCR；新增取消仅停止尚未开始的图片，当前图片会自然完成。

## 2026-08-19 · 新增简明操作手册

- 新增面向第一次使用用户的《操作手册》，覆盖导入截图、OCR 识别、城市设置、联网识别、表格检查、导出 Excel、汇总表、查重和常见问题。

## 2026-08-19 · 城市人工设置交互调整

### 调整
- 识别完成后不再自动弹出未识别店铺的城市分配窗口，避免中断查看 OCR 结果。
- 新增“统一设置店铺城市”按钮，仅在用户主动点击时批量处理当前表格中尚未设置城市的店铺。

## 2026-08-19 · Windows 本地匹配与城市范围加固

### 修复
- 新学习库未命中时，兼容读取用户电脑已有的 `shop_city.db` 历史记录；旧库仅作只读回退，不会自动写入或污染学习库。
- 本地学习库和旧库结果均强制按用户选择的城市范围过滤，避免东莞、佛山等批次互相串城。
- 联网识别增加工作线程和表格回填两道范围校验；即使下游服务返回范围外城市，也会拒绝写入表格并记录日志。
- 空的限定城市集合不再被解释为“全部城市”，杜绝未来调用路径绕过范围限制。

### 验证
- 新增旧库兼容、范围外旧库拒绝、范围外联网结果拒绝、空范围拒绝共 4 项回归测试。

## 2026-08-19 · 城市匹配范围修复

### 修复
- 恢复旧版历史本地库迁移记录的 L1-L4 高可信匹配，避免本地数据库无命中。
- 联网识别仅采纳用户所选城市范围内的高可信本地结果；范围外和模糊候选继续走受限查询，避免跨城写入。

## 2026-08-19 · Windows 打包版运行日志

- 新增 `%LOCALAPPDATA%\\LQPriceCheck\\logs\\application.log` 滚动日志。
- 记录启动自检、OCR 每张图片、字段解析、表格生成和未捕获异常。
- 识别或表格生成失败时直接提示日志路径，便于反馈问题。

### 修复
- 修复批量 OCR 完成最后一张图片后因完成回调路径为 `None` 导致程序闪退的问题。
- macOS 与 Windows 的进度回调统一兼容批量完成通知。
- 启用系统 DPI 缩放，并让主窗口根据当前屏幕可用区域自动设置首次大小。
- 巡查表和汇总明细表保留 M/N 计算公式，并在 Excel/WPS 打开时自动重新计算。

## 2026-08-18 · macOS / Windows 双平台企业化基础优化

### 修复
- 统一使用用户可写的数据目录，避免 PyInstaller `_MEIPASS` 或源码目录不可写导致知识库失效。
- 统一知识库业务层，移除主界面对旧 `shop_city.db` 的双写路径；旧库仅保留一次性迁移用途。
- 投喂批次先创建，再关联每条人工修正记录，补齐审计链。
- 同一 OCR 别名归属不同店铺时标记冲突，避免静默覆盖已有知识。
- 启用 SQLite 外键约束，并记录部分失败的 Excel 投喂批次。

### 兼容性
- macOS 继续使用 Vision OCR，Windows 继续使用 RapidOCR；OCR 引擎之外的知识库、匹配和导入逻辑保持一致。
- Windows 保留启动前的平台、资源、VC++ 和 OCR 组件自检。
- 导出目录打开逻辑改为按操作系统选择实现。


---

## 2026-07-08 · 项目启动

### 新增
- 搭建项目骨架：PyQt5 GUI 主程序 `main.py`
- 实现 macOS Vision OCR 引擎 `ocr_engine.py`（内嵌 Swift 脚本调用 `VNRecognizeTextRequest`）
- 实现字段解析器 `field_parser.py`：基于 OCR 文本坐标的规则匹配，提取店铺名、产品名、价格等 16 个字段
- 实现 Excel 写入器 `excel_writer.py`：基于模板复制，保留格式，自动写入 M/N 列公式
- 实现城市池 `city_pool.py`：覆盖广东、广西、海南、贵州 4 省地级市
- 实现城市识别 `city_detector.py`：三层策略（店名提取 / 百度地图搜索 / 关键词推断）
- 创建 Excel 模板 `模板.xlsx`（4 个品牌子表）
- 实现拖拽导入、批量选择、zip 压缩包导入三种截图导入方式
- 实现 OCR 后台线程 `OCRWorker(QThread)`，识别过程不阻塞 UI

### 功能
- 选择省份 -> 城市，自动填入所属区域
- 导入截图 -> OCR 识别 -> 表格编辑 -> 导出 Excel 全流程
- 支持燕京/雪花/青岛/百威 4 品牌自动分表导出

---

## 2026-07-09 · 品牌识别增强 + 合格率统计

### 新增
- 实现产品名称标准化：按品牌分类（漓泉/燕京/雪花/青岛/百威），统一命名规范
- 实现品牌自动识别 `detect_brand()`：按产品名关键词分到 4 个子表
- 实现「按店铺批量设置城市」功能：列出所有店铺，批量选择城市
- 实现 `qualification_generator.py`：价格合格率统计表生成器（漓泉1998 / 燕京U8）
- 实现 `price_stats.py`：早期版本的合格率统计（后被 qualification_generator 替代）
- 汇总表支持分省汇总 + 分地级市汇总
- 城市池新增云南省（16 个地级市），总计覆盖 5 省

### 修复
- 修复 OCR 价格误读：`¥461~` 实际为 `4.61`，3 位数 + 波浪号自动除以 10
- 修复配送费提取：区分打包费和配送费，合并为 L 列

---

## 2026-07-10 · 汇总表重写 + 产品名称修正 + 输出结构优化

### 新增
- **重写汇总表生成器 `summary_generator.py`**：按用户新模板完全重写
  - Sheet 1「汇总表」：分省汇总 + 分地级市汇总（上下排列）
  - Sheet 2「核查明细表」：完整 16 列巡查表格式，不合格行整行标红（字体 `FFFF0000`）
  - 深蓝标题行 `FF003366` + 蓝色表头行 `FF0070C0`
  - 合格标准行补充理论成交价定义：`产品理论成交价格=产品成交价格-打包、配送费`
- **汇总表 M/N/O 列生成**：
  - M 列（产品理论成交价格 = G−L）：源文件公式无缓存值时手动计算兜底
  - N 列（去除平台优惠价 = G+J+K−L）：同上手动计算兜底
  - O 列（照片）：WPS DISPIMG 格式嵌入截图（`_inject_wps_cellimages` 注入 cellimages.xml）
- **合格判定改为基于 M 列**：理论成交价（成交价 − 配送费）≥ 合格线即为合格
- **输出目录按批次分子文件夹**：每次导出在 `output/` 下创建 `巡查表_YYYYMMDD_HHMMSS/` 子文件夹，巡查表与汇总表集中存放

### 修复
- **雪花啤酒 清爽/勇闯 混淆**：`8°P清爽` 被误判为清爽，实际应为勇闯。修复：检测到"清爽"关键词时，若同时存在 `°P`（带 P）则归为勇闯，仅 `°` 则归为清爽
- **老雪规格**：OCR 无容量信息时默认 500ml，修复为老雪固定 `640ml*12瓶`
- **青岛 110P OCR 误读**：OCR 将 `11°P` 识别为 `110P`，修复用正则 `(\d{2})0[Pp]` 提取前两位
- **燕京 U88 OCR 误读**：OCR 将 `U8 8°P` 识别为 `U88°P`，修复 `if num == "88": num = "8"`
- **青岛白啤无关键词**：标题 `【整箱】青岛啤酒 110P小.` 无"白啤"文字，修复：度数为 11 时默认归为白啤（11° 是白啤标志度数）
- **规格从缩略图提取**：OCR 缩略图区域（left < 0.12）的"12听装"与规格行"12瓶"冲突，修复：提取缩略图标签为 extra_text，"X听装"模式优先
- **WPS cellimages 图片提取**：`_extract_images_from_xlsx` 成功提取 70 张图片，汇总表文件从 9KB 增至 11.5MB

### 优化
- **产品名称标准化签名扩展**：`_normalize_product_name(title, spec, sub_title="", extra_text="")`，合并副标题和缩略图文本用于度数检测
- **雪花系列细分**：superX / 老雪 / 勇闯 / 清爽四类，各按度数细分
- **青岛系列细分**：原浆 / 奥古特 / 2000 / 白啤 / 纯生 / 经典 / 清爽七类
- **百威系列细分**：纯生 / 铝罐 / 普通三类，按度数细分

### 删除
- **移除「合格率统计表」功能**：该功能已被「汇总表」完全替代
  - 删除 `qualification_generator.py`
  - 删除 `price_stats.py`（更早期版本，无引用）
  - 删除 `main.py` 中的「📊 生成合格率统计表」按钮及 `_generate_qualification()` 方法
  - 删除 `import qualification_generator`
- **清理 output/ 旧文件**：删除所有旧的散乱巡查表、汇总表、统计表文件

### 验证
- 产品名称标准化：67/70（95.7%）与用户手动修正的 Excel 一致
- 3 条不一致为 OCR 文本限制（截图中无规格信息），非逻辑问题
- 汇总表生成测试：21 条记录，10 条不合格标红，21 张图片嵌入，文件 11.5MB
- 输出子文件夹测试：巡查表与汇总表正确放入同一子文件夹
## 2026-09-02 · 跨平台城市决策与 OCR 异常恢复

### 修复
- macOS 与 Windows 共用证据型城市决策：保留完整店名/分店名和百度 POI 证据，按证据得分及候选分差保守决策。
- Windows RapidOCR/onnxruntime 初始化异常时转为可重试记录并发出完成信号，避免 EXE 界面永久停留在“识别中”。

### 验证
- Windows 单元回归 23 项、Python 编译检查和 `git diff --check` 通过。
