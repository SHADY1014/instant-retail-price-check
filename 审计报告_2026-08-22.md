# 价格核查软件包审计报告

审计范围：Windows Python 桌面端、Flutter Android 端、打包与内部发行配置。未将操作手册和项目内历史说明当作本次需求指令。

## 已修复

1. 学习库回归测试改用唯一临时目录，避免固定 `/tmp/test_ocr_learning.db` 被旧文件权限污染。
2. 移动端店铺城市批量查询由逐条 SQLite 查询改为分块 `IN` 查询，批量学习保存改为单事务；大批量重新匹配时减少数据库往返和写入提交。
3. 移动端 OCR 和导出流程补齐异常与 `mounted` 保护。识别异常后按钮会恢复可用，避免界面永久停留在“识别中”。
4. Android release 构建移除调试证书回退，要求显式配置 release keystore，并忽略私钥配置。
5. Windows 构建新增版本文件和 SHA-256 发布清单，建立可审计的内部分发基础。

## 高优先级待办

1. Windows EXE 目前没有企业代码签名。未签名的 PyInstaller 文件容易被终端防护拦截，也无法建立发行者信任；应在发布构建机完成 Authenticode 签名和验签。
2. 项目根目录不是 Git 仓库，且 Windows、macOS、移动端分别复制了大量解析逻辑。应迁入一个内部 Git 仓库，设置主分支保护、发布标签和变更审查；将共用规则数据化，避免三端修复不同步。
3. Flutter/Gradle 的发布验收应在可写缓存的 Windows 构建机执行，包含 `flutter analyze`、release APK 构建、签名验证和真机验收。

## 体验与逻辑建议

- 桌面端顶部一次性放置 9 个操作按钮，低分辨率时阅读和操作负担较重。建议按“导入与识别 / 城市核对 / 导出与汇总 / 数据管理”分组，并把低频功能收进菜单。
- 识别结果应增加“需核对”状态：店铺、产品、成交价、城市任一缺失或低置信度时明确标记并支持筛选，导出前显示未核对条数。
- 城市联网识别应持久化记录用户授权、请求时间、候选和最终人工裁决，以便数据争议追溯；仅上传店名且维持现有区域白名单限制。
- OCR、汇总和图片嵌入均为耗时操作，应提供可取消任务、失败条目重试和成功/失败计数，避免整批任务因单张异常缺少可恢复路径。

## 验证记录

- Windows 城市范围回归测试：4/4 通过。
- Flutter 单元测试：2/2 通过。
- 学习库脚本修复后：24/24 通过；城市范围回归：4/4 通过。
- Flutter/Gradle 静态分析需要在可写 Flutter/Gradle 全局缓存的发布机复跑；当前受限环境不允许这些工具更新缓存。
