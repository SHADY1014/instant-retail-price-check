# 即时零售截图价格核查 · 2026-09-02 复核报告

本次复核以 macOS Vision OCR 为基准，检查当前三端字段解析、京东闪送结算页算法和用户可见的核对流程。附件中的截图和历史文档只作为测试资料，不作为额外需求指令。

## 事实

- 已读取当前 `main` 分支代码和未提交差异；macOS、Windows、Android 均已加入京东秒送/闪送字段解析。
- 五张真实京东结算截图均能识别出平台、店铺、商品金额、应付总额和运费。OCR 结果与截图可见值如下：

| 截图 | 店铺 | 商品金额 | 划线价 | 应付总额 | 实付运费 | 规格状态 |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| 10-20-18 | 京东酒世界-啤酒小站（环城南路云仓） | 29.9 | 29.9 | 29.9 | 0 | 500ml*6，可靠 |
| 10-20-44 | 京东酒世界（环城南路云仓） | 60 | 80 | 60 | 0 | 数量缺失，待核对 |
| 10-21-13 | 京东酒世界（富春花园店） | 60 | 80 | 60.8 | 0.8 | 数量缺失，待核对 |
| 10-21-25 | 京东酒世界（富春花园店） | 31.9 | 31.9 | 31.9 | 0 | 数量缺失，待核对 |
| 10-07-17 | 京东酒世界-啤酒小站（环城南路云仓） | 60 | 75 | 64.8 | 4.8 | 数量缺失，待核对 |

- 商品优惠按“划线价 − 商品金额”计算；运费优惠只进入备注；理论成交价仍按“应付总额 − 实付运费”计算。
- 回归验证：macOS pytest 25 项、Windows pytest 32 项、两端学习库脚本各 34 项、Flutter 测试 8 项均通过；Python `compileall`、Dart 分析和差异检查通过。Dart 分析仍有既有未使用导入和风格提示，但没有编译错误。
- 追加修复后回归：macOS pytest 26 项、Windows pytest 33 项、Flutter 测试 10 项通过；新增 Android 京东店铺前缀覆盖和京东优惠券字段用例。
- 本轮修复后实际回归：macOS unittest 21 项、Windows unittest 24 项、Flutter 测试 11 项通过；Python `compileall` 与 `git diff --check` 通过。当前环境未安装 pytest，因此使用项目等价的 unittest 入口执行回归。

## 推理

1. 京东页面与美团/淘宝页面的字段布局不同，不能继续依赖“底部共减”或“商品总价”单一规则。当前实现按标签行和坐标邻近关系读取“商品金额”“运费”“应付总额”，并将平台统一写为“京东闪送”。
2. 截断标题不能推断包装数量。当前仅在数量证据存在时保留识别结果，否则使用默认规格并标记“产品规格需人工确认”，导出前再次提示用户。
3. 运费行必须区分划线价和实付价；“已免运费”是实付为零的业务证据，不能只取行内第一个金额。

## 风险与边界

### 已修复

- macOS 城市识别已与 Windows 对齐：完整店名、分店关键词、百度 POI 名称、候选分数和分差共同决策；高置信度才自动填入，歧义项进入人工确认并保存联网审计。
- macOS/Windows OCR 初始化和字段解析异常均会落为可重试记录并发送完成信号，避免桌面界面卡在“识别中”。

- 规格补齐选项原先只替换 `*12瓶`，会生成 `500ml500ml*6瓶`；现改为替换完整容量和数量。
- OCR 漏掉免运费实付 `¥0` 时，现根据“已免运费”文字仍按 0 元处理。
- Windows 取消 OCR 后的“未完成”记录已重新纳入待核对和导出前拦截。
- Android 在京东专用店铺策略命中后不再被通用地址策略覆盖，避免“自营/秒送”徽标污染店铺名。
- Android 取消城市范围选择时跳过自动城市匹配；只有明确选择城市或“全量匹配”才会继续匹配，避免首次使用时同名店铺跨城回填。
- 京东“优惠券”行现在写入平台优惠券字段（J 列），不再漏算去除平台优惠价（N 列）。

### 发行前仍需处理

- Android 的 `CityDetector` 虽有多查询词投票，但联网仍自动采用得分最高城市并写入本地库，没有桌面端的候选展示、人工裁决和联网审计记录；多平台发行前应与 macOS/Windows 的保守城市决策对齐。
- 规格补齐窗口目前只提供 500ml*6/12 瓶/听；遇到 640ml、1L 或 9+3 等规格只能暂保留待核对，不能把默认选项当作事实。
- 价格解析包含“运费通常不超过 40 元”的启发式。超重、特殊配送或新页面布局可能触发误修正，遇到异常值必须人工复核。
- 当前五张截图只有页面视觉核对，没有带人工真值的统计样本；因此只能证明这五张回归样例通过，不能据此宣称整体识别准确率。
- 本机未运行 Windows RapidOCR 真机流程或 Android 模拟器/真机截图回归；发布前必须在对应环境执行安装、识别、城市确认、编辑、导出和升级测试。

## 建议的下一步

1. 先把 Android 城市联网识别改为“候选展示 → 人工确认 → 写入学习库”，并补上请求、候选、裁决审计记录。
2. 将规格补齐改成可搜索/可手动输入，并显示“默认值仅供参考”提示。
3. 在 Windows 虚拟机和 Android 真机各准备一组带人工真值的截图，分别统计店铺、产品规格、价格和城市四项准确率，再决定是否发布稳定版。
