# 内部发行与更新方案

## 发行原则

- Windows 使用企业代码签名证书签名 `PriceCheckOCR.exe`，Android 使用专用 release keystore 签名 APK；调试签名包不得进入发行渠道。
- 每次发布只能发布同一目录中的安装包、`release-manifest.json`、变更说明和 SHA-256 校验值。
- 版本号使用三段式：修复为补丁版本，新增兼容功能为次版本，数据口径或导出模板不兼容时为主版本。
- 先向 5 到 10 名试点用户发布 `pilot`，运行 3 个工作日无阻断问题后再提升至 `stable`。

## Windows 发布

1. 在专用 Windows 构建机运行 `build_windows.bat`。
2. 用企业证书对 `dist\\PriceCheckOCR.exe` 签名并验证签名。
3. 构建脚本会根据 `VERSION` 生成 `dist\\release-manifest.json`，其中包含文件名、大小和 SHA-256。
4. 将 EXE、清单和 `CHANGELOG.md` 上传至内网只读发布目录，例如 `\\intranet\\software\\lq-price-check\\stable\\`。
5. 下载页或 MDM 策略只显示清单指定的版本；用户更新前先比对 SHA-256，保留上一个稳定版供回滚。

## Android 发布

1. 由发行管理员创建并离线保管 keystore；将 `android/key.properties.example` 复制为 `android/key.properties` 并填写本机路径和密码。该文件及 keystore 已被忽略。
2. 执行 `flutter build apk --release`。没有 release keystore 时，构建会明确失败，不再退回到调试签名。
3. 在真机完成导入、OCR、城市限制、编辑、导出和分享验收后，在软件包根目录执行以下命令生成 APK 清单：

   ```bash
   python3 screenshot_ocr_windows/tools/create_release_manifest.py \
     screenshot_ocr_mobile/build/app/outputs/flutter-apk/app-release.apk \
     --app-id com.lqpricecheck.price_check_ocr \
     --version 1.0.1+2 \
     --output screenshot_ocr_mobile/build/app/outputs/flutter-apk/release-manifest.json
   ```
4. Android 的同一 applicationId 只能安装由同一 keystore 签名、且 `versionCode` 更高的更新包；升级前必须递增 `pubspec.yaml` 的 `version`。

## 更新检查和回滚

第一阶段采用内网下载页或 MDM 推送，避免客户端在没有证书校验、代理规则和网络权限约束时自行下载并替换可执行文件。第二阶段若需应用内提醒，客户端只读取 HTTPS 上的清单，展示版本、说明和下载链接；文件下载和替换交给 MDM 或独立安装器执行，并在安装前校验清单中的 SHA-256。

发布目录始终保留最近两个稳定版和一个当前试点版。出现识别口径、导出模板或数据迁移问题时，先暂停清单指向的新版本，再让 MDM/安装器回退到上一个签名且校验通过的稳定包。用户学习库位于应用数据目录，升级包不得覆盖它。
